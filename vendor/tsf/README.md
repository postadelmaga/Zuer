# TinySoundFont (tsf.h + tml.h)

Vendored da https://github.com/schellingb/TinySoundFont (branch master, commit fbc9135 del 2025-07-19). Licenza MIT — synth SoundFont2 (`tsf.h`) e parser MIDI SMF (`tml.h`); `tsf_impl.c` compila entrambe le implementazioni.
